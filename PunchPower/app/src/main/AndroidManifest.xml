<?xml version="1.0" encoding="utf-8"?><!-- suppress AndroidDomInspection -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="app.example.juanjo.PunchPower">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.hardware.sensor.accelerometer" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ControlEscenas" />
        <activity android:name=".Escenas" />
        <activity android:name=".PantallaInicio" />
        <activity android:name=".ControlEscenas" />
        <activity android:name=".PantallaPersonajes" />
        <activity android:name=".PantallaJuego" />
        <activity android:name=".PantallaAyuda" />
        <activity android:name=".PantallaOpciones" />
        <activity android:name=".PantallaDatos" />
        <activity android:name=".ListadoJugadores" />
        <activity android:name=".AdaptadorListaRecord" />
    </application>

</manifest>